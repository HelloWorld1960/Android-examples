<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">
    <!--Action Bar esta descontinuado.
        ToolBar es lo mas usado.-->

    <!--Propiedades para un ActionBar. -->
    <!--android:icon: Le asigna un icono. -->
    <!--app:showAsAction="" Indica como se muestran en el ActionBar
        never:nunca.
        always:siempre
        ifRoom: Si queda lugar.-->
    <!--app:showAsAction="never" Indica que siempre se mostrara en el ActionBar -->
    <!--android:orderInCategory: Sirve para darle un orden en el ActionBar -->
    <!--
    <item
        android:id="@+id/config"
        android:icon="@android:drawable/ic_menu_preferences"
        android:title="Configuracion"
        app:showAsAction="never" />
    <item
        android:id="@+id/info"
        android:icon="@android:drawable/ic_menu_info_details"
        android:title="Informacion"
        app:showAsAction="ifRoom"
        android:orderInCategory="10"/>
    <item
        android:id="@+id/buscar"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Buscar"
        app:showAsAction="always"
        android:orderInCategory="5"/>
    -->

    <!--Propiedades -->
    <item
        android:id="@+id/config"
        android:icon="@android:drawable/ic_menu_preferences"
        android:title="Configuracion" />
    <item
        android:id="@+id/info"
        android:icon="@android:drawable/ic_menu_info_details"
        android:title="Informacion" />
    <item
        android:id="@+id/buscar"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Buscar" /> -->

</menu>